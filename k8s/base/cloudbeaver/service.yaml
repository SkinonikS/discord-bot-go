apiVersion: v1
kind: Service
metadata:
  name: cloudbeaver
  namespace: discord-bot
spec:
  selector:
    app: cloudbeaver
  ports:
    - port: 8978
      targetPort: http
      nodePort: 30300
  type: NodePort

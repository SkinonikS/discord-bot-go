app:
  name: "discord-bot"
  debug: ${APP_DEBUG}
logger:
  level: "${LOG_LEVEL:debug}"
  disable: ${LOG_DISABLE:false}
discord:
  appID: "${DISCORD_APP_ID}"
  token: "${DISCORD_TOKEN}"
  intents:
    - Guilds
    - GuildVoiceStates
    - GuildMessageReactions
